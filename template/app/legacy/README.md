# Retired starter modules

This directory preserves the original starter payment, generic file upload, and
AI scheduler demo code for reference. It is intentionally outside `src`, is not
registered in `main.wasp.ts`, and is not included in the MotionPress build.

Do not move these modules back into the runtime without a new product decision,
threat review, server-side authorization, and dedicated integration tests.
